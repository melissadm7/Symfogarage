{% extends 'base.html.twig' %}

{% block title %} Modification de {{ app.user.fullName }}{% endblock %}

{% block body %}
<div class="slide">
    <div class="container">
        <h1>Modification du profil</h1>
        {{form_start(myForm)}}
            {{form_widget(myForm)}}
                <button type="submit" class="btn btn-success">Enregistrer les modifications</button>
        {{form_end(myForm)}}
    </div>
</div>
{% endblock %}